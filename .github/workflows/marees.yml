name: Marées

# L'API n'expose que la fenêtre J-30 à J+30 : un rafraîchissement quotidien maintient
# en permanence un mois de prévisions d'avance à disposition de l'app.
on:
  schedule:
    - cron: '17 4 * * *'
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

# Une seule publication à la fois, sans annuler celle en cours.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  publier:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploiement.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Générer marees.json
        env:
          API_MAREE_KEY: ${{ secrets.API_MAREE_KEY }}
        run: node marees.mjs

      # Le site et les marées partagent une publication unique : les pages statiques doivent
      # donc rejoindre l'artefact à chaque exécution, sans quoi la mise à jour quotidienne des
      # marées les ferait disparaître.
      - name: Ajouter les pages du site
        run: cp -R pages/. site/

      - uses: actions/configure-pages@v6

      - uses: actions/upload-pages-artifact@v5
        with:
          path: site

      - id: deploiement
        uses: actions/deploy-pages@v5
